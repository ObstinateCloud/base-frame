server:
  port: 8090
spring:
  profiles:
    active: nanri
  jpa:
    show-sql: true


#mybatis-plus 打印日志
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
logging:
  level:
   tracer: trace
  file:
    path: log

